name: Logo grid
icon: layers
fields:
  logogrid:
    label: Select logo grid
    type: select
    options: query
    query: site.children.template("logogrid")
  placement:
    label: Block placement
    type: radio
    options:
      inFront: "In front"
      behind: "Behind"
    default: inFront